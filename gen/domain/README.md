# Generated Domain Types

This directory contains domain types used by the mapper.
These files bridge Kubernetes CRD types and protobuf API types.

**Do not edit these files manually.**

To regenerate them, run the code generation targets in the repository (e.g. `make generate` or `buf generate`).
